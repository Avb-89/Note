## ===============CentOS====================
На CentOS столкнулся с тем, что новые пользователи 
не могут зайти по ключу, исправляется так:

	1) Залогинится под пользователем
	2) chmod -R go= ~/.ssh; chmod go=~
    Возможно только chmod -R go= ~/.ssh
## =========================================